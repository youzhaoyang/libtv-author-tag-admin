{
  "name": "libtv-author-tag-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:vercel": "vite build --base /",
    "build:preview": "vite build --config vite.preview.config.js",
    "preview:serve": "npm run build:preview && npx serve docs -p 4173 --cors",
    "preview": "vite preview",
    "deploy": "npm run build:preview"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-image-crop": "^11.0.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-react": "^4.4.1",
    "gh-pages": "^6.2.0",
    "vite": "^6.2.2",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
